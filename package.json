{
  "name": "ironlog",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted lifting and body progress dashboard",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  }
}
